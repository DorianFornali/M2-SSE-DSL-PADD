application simpleAlarm

# Declaring bricks
sensor button: 9
actuator led: 12

# Declaring states
on {
    led <= HIGH
    => off {
        button is LOW
    }
}

-> off {
    led <= LOW
    => on {
        button is LOW
    }
}
